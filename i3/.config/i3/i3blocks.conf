# i3blocks configuration example

[time]
command=date '+%Y-%m-%d %H:%M:%S'
interval=1

[cpu]
command=top -bn1 | grep "Cpu(s)" | sed "s/.*, *\([0-9.]*\)%* id.*/\1/" | awk '{print 100 - $1 "%"}'
interval=2

[volume]
command=amixer get Master | grep -oP '\[\K[0-9]+(?=%\])'
interval=10
